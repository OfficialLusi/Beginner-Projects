<Window x:Class="BloggingPlatform_FE.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BloggingPlatform_FE.Views"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="800">
    <Grid Margin="150,100,150,100" Background="AliceBlue">
        <StackPanel VerticalAlignment="Center" Margin="100,0,100,0">
            <TextBlock Text="Insert you credentials" FontSize="16" Margin="0,0,0,20" HorizontalAlignment="Center"/>
            
            <!--Email and password "binding"-->
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Width="250"/>
            <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10" Width="250"/>
            
            <!--Login button-->
            <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}" Margin="0,10,0,5" Width="50"/>
            
            <TextBlock Text="Or if you don't have an account:" FontSize="10" Margin="30,2,30,2" HorizontalAlignment="Center"/>
            
            <!--Signup button-->
            <Button Content="Sign Up" Command="{Binding NavigateToSignupCommand}" Margin="0,5,0,0" Width="50"/>
            
        </StackPanel>
    </Grid>
</Window>
